import java.io.File;
import java.util.Scanner;

public static void main(String[] args) {
    Scanner scanner = new Scanner(System.in);
    int Count = 0; // Переменная счетчика  файлов

    while (true) {
        System.out.println("Введите путь к файлу для парсинга (или 'exit' для выхода):");
        String path = scanner.nextLine(); //ввод пути или стоп слова

        if ("exit".equals(path)) {  /*equals() — это метод сравнения строк в Java, который проверяет содержимое строк на идентичность.
🔍                                   Что делает equals()? Сравнивает строки посимвольно
                                     java "exit".equals("exit") // true, "exit".equals("Exit") // false (регистр имеет значение)
                                     equals()	Сравнивает содержимое строк	"Hi".equals("Hi") → true
                                     ==	Сравнивает ссылки на объекты	new String("A") == "A" → false*/
            System.out.println("Выход из программы.");
            System.out.println("Всего верно указанных файлов: " + Count);
            break;
        }

        if (path.isEmpty()) {  /*isEmpty() — это метод в Java, который проверяет, является ли строка пустой (не содержит ни одного символа)
                                Как работает isEmpty()?Метод возвращает:
                                true – если строка имеет длину 0 (т.е. "").false – если в строке есть хотя бы один символ (даже пробел)*/
            System.out.println("Путь не может быть пустым. Попробуйте снова.");
            continue;           /*continue пропускает текущую итерацию цикла и переходит к следующей. В задаче:
                                    Если path.isEmpty() вернет true, программа:
                                    Выведет сообщение "Путь не может быть пустым...",
                                    Перейдет к следующему шагу цикла (continue),
                                    снова запросит ввод (scanner.nextLine()).*/
        }


        File file = new File(path); /* гугл:Создание объекта File: File file = new File("путь/к/файлу.txt");
                                    "путь/к/файлу.txt" = path из сканера String path = scanner.nextLine();*/


        if (!file.exists()) {       //гугл: Проверка существования файла if (file.exists()) {  System.out.println("Файл существует");
            System.out.println("Файл не существует.");
            continue;
        }

        if (file.isDirectory()) {   /*Метод isDirectory() в Java используется для проверки, является ли файловый объект директорией (папкой) или обычным файлом.
                                    Как работает:
                                    · true - если объект является директорией
                                    · false - если объект является обычным файлом или не существует */

            System.out.println("Указанный путь ведет к директории, а не к файлу. Попробуйте снова.");
            continue;
        }

        Count++; // Увеличиваем счетчик
        System.out.println("Путь указан верно.Это файл номер N  "+Count);

        }

    scanner.close();
}



